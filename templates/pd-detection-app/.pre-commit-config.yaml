# Optional: pip install pre-commit && pre-commit install
# Align CI in this repo with gitleaks (see WeAdU-ltd/.github .github/workflows/ci.yml).
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
