# Local tooling / Python
__pycache__/
*.py[cod]
.pytest_cache/
actionlint
actionlint.tar.gz

# SSH private keys (never commit)
*.pem
*.ppk

# Local 1Password env for OVH inventory (never commit real op:// paths to a public fork by mistake)
scripts/ovh_inventory_wea28.op.env

# Local Gmail OAuth (WEA-24) — copy from .env.gmail.local.example
.env.gmail.local

# Local env (ex. LM_STUDIO_BASE_URL, Docker) — ne pas versionner
.env

# Fichier d'audit orchestrateur IA (WEA-178) si AI_ORCHESTRATOR_JSONL pointe ici
ai_orchestrator.jsonl
